# Сквозная редактура 7-11 - русификация мотивационного словаря

Дата: 2026-05-25.

## Область прохода

Проверены и точечно отредактированы главы мотивационного блока:

- [[../Главы/07-Мотивация-это-не-желание]];
- [[../Главы/08-Четыре-области-мотивации]];
- [[../Главы/09-Приближение-и-избегание]];
- [[../Главы/10-Управляемость-действия]];
- [[../Главы/11-Цена-усилия-усталость-и-ощущаемая-энергия]].

Цель прохода - убрать из читательского слоя и источниковых блоков черновую англо-русскую смесь, особенно там, где английские научные ярлыки уже имеют нормальные русские учебные формулировки.

## Что исправлено

- `self-efficacy` заменено на "самоэффективность".
- `competence in SDT` заменено на "компетентность в теории самоопределения".
- `approach/avoidance` заменено на "приближение и избегание".
- `avoidance learning` заменено на "обучение избеганию".
- `mastery experience` заменено на "опыт успешного овладения".
- `learned helplessness` заменено на "выученная беспомощность".
- `learned controllability` заменено на "выученная контролируемость".
- `controllability over stressor` и `threat-related areas` заменены на "контролируемость стрессора" и "области, связанные с угрозой".
- `illusion of control` заменено на "иллюзия контроля".
- `effort-based decision making` заменено на "решения на основе цены усилия".
- `adaptive cost-benefit control` заменено на "адаптивный контроль затрат и выгод".
- `opportunity cost` заменено на "альтернативная стоимость".
- `expected value of control` заменено на "ожидаемая ценность контроля".
- `fatigue-as-cost-shift` заменено на "понимание усталости как сдвига цены".
- `computational` заменено на "вычислительный".
- Нестандартные локальные метки `theory-building`, `authorial-practice` и `boundary` в главах 7-8 заменены на русские формулировки: "теоретическая сборка", "авторская практика" и "граница применимости".

## Что не менялось

- Структура мотивационного блока и статус глав.
- Авторско-годовые ссылки.
- Стандартные evidence labels из библиографического аппарата: `strong`, `context-dependent`, `fast-moving`, `clinical-boundary`. Они оставлены до отдельного решения по финальной верстке доказательных ролей.

## Результат

Блок 7-11 стал ровнее как русскоязычная учебная лестница:

```text
мотивация не равна желанию
-> ценность бывает разной
-> ценность может вести к приближению или избеганию
-> действие требует управляемого рычага
-> даже управляемое действие имеет цену
```

Главы не переводились в `done`: этот проход закрывает языковой слой, но не заменяет финальную сверку claims vs evidence labels, общую редактуру повторов и сквозное чтение всей книги.

## Проверки

- Целевой поиск по англоязычным хвостам `self-efficacy`, `mastery experience`, `learned helplessness`, `approach/avoidance`, `effort-based decision making`, `opportunity cost`, `fatigue-as-cost-shift` в главах 7-11 не дал совпадений.
- `git diff --check` прошел без замечаний.
- По главам 7-11 не найдено trailing whitespace.

# Состояние извлечения беседы

## Исходная ссылка

- Shared-беседа: <https://chatgpt.com/share/6a3f9199-9cf8-83eb-b11b-14f46b0e0919>
- Backend API: <https://chatgpt.com/backend-api/share/6a3f9199-9cf8-83eb-b11b-14f46b0e0919>

## Что удалось сохранить

2026-06-27 прямой backend API через прокси `http://127.0.0.1:8899` вернул `200 application/json`. HTML/RSC fallback не потребовался.

Параметры выгрузки:

- заголовок в ChatGPT: `Поиск новых решений`;
- размер backend JSON: 2435727 байт;
- узлов в цепочке текущей беседы: 215;
- всего узлов в `mapping`: 215;
- элементов `linear_conversation`: 215;
- сохранено видимых текстовых ходов ролей `user` и `assistant`: 24;
- элементов `safe_urls`: 144;
- исключено `system`-узлов: 6;
- исключено скрытых placeholders custom instructions: 1;
- исключено заглушек редактированных tool/research-выводов: 83;
- исключено tool/code/web-вызовов: 54;
- исключено reasoning/thoughts-узлов: 45;
- визуально скрытых узлов в цепочке: 9.

Из backend JSON сохранены:

1. `01-Извлеченная-беседа.md` - видимые текстовые сообщения `user` и `assistant` после фильтрации служебных и редактированных узлов.
2. `06-Ссылки-из-backend-json.md` - сырой список URL из `safe_urls`.

Пользователь приложил research-результат отдельно, потому что он может быть недоступен в shared-ссылке. Он сохранен как `02-Research - поиск новых решений.md`.

## Что не удалось извлечь

Полный research-ответ не представлен в публичном backend JSON как обычный текстовый assistant-узел: в цепочке есть технические заглушки `The output of this plugin was redacted.`. Поэтому основная evidence-база материала взята из пользовательского вложения, а не из shared JSON.

В файл `01-Извлеченная-беседа.md` намеренно не включены служебные элементы `system`, `tool`, скрытые custom-instruction placeholders, внутренние reasoning/thoughts-узлы, tool/web-вызовы в формате `code` и технические заглушки вместо tool/research-выводов.

Сырой список ссылок из `safe_urls` содержит технические, рекламные и промежуточные элементы. Для книги его нельзя использовать как готовую библиографию без проверки по DOI, PubMed/PMC, издательским страницам, университетским архивам и официальным документам.

## Команда извлечения

```powershell
curl.exe -x http://127.0.0.1:8899 -L --show-error --silent `
  --output "$env:TEMP\chatgpt-share-6a3f9199-9cf8-83eb-b11b-14f46b0e0919.json" `
  --write-out "%{http_code} %{content_type} %{size_download}" `
  -H "User-Agent: Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/125.0.0.0 Safari/537.36" `
  -H "Accept: application/json, text/plain, */*" `
  -H "Referer: https://chatgpt.com/share/6a3f9199-9cf8-83eb-b11b-14f46b0e0919" `
  "https://chatgpt.com/backend-api/share/6a3f9199-9cf8-83eb-b11b-14f46b0e0919"
```

Результат: `200 application/json`.

## Риск для будущего агента

Будущий агент может считать этот dossier рабочей копией видимой части shared-беседы и полного приложенного research-пакета. Но он не должен считать, что публичная shared-ссылка содержит весь research-артефакт, и не должен принимать сырой `safe_urls` за готовую библиографию.
